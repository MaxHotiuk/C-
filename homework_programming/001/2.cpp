#include <iostream>
using namespace std;

int main()
{
    float x, y, z, n;
    int in;

    cin >> x;
    cout << endl;
    cin >> y;
    cout << endl;
    cin >> z;
    cout << endl;

    in = ((x + y + z) / 3);
    n = ((x + y + z) / 3);

    cout << n - in << endl;


}