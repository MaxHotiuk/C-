#include <iostream>
using namespace std;

int main()
{
    int k;
    double x, y, z;
    bool bol;
    if (k % 7 == 0) {
        bol = 1;
    } else if (x > 0 && x < 1){
        bol = 1;
    } else if (x == max(x, y, z)) {
        bol = 1;
    }
}